/**
 * Shared domain types for the cafe retention demo.
 *
 * This is the single place where teams should define types that are shared
 * across surfaces: Customer, Product, Transaction, Offer, Experiment,
 * Assignment, Outcome, Signal, DisengagementReason, Intervention, and
 * Feedback. It is intentionally empty until the data team agrees on the first
 * draft.
 *
 * Ownership: data and experiment logic team.
 */

// Placeholder export so this module has a stable shape to build against.
export type DomainMarker = {
  note: string;
};
